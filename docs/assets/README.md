# Assets

- `pum-human-first-social.png` — README/social hero generated 2026-07-23. It puts a
  calm human operator, a local command centre, source convergence, and verified states
  ahead of generic "cybersecurity" imagery. The title must visibly expand the acronym:
  **PUM — Package Update Manager**.

  Exact generation prompt:

  ```text
  Edit the supplied PUM social-preview image into a premium GitHub hero, wide 2:1
  composition. Preserve the human-centered calm dark scene: one person at a laptop in a
  warm low-light studio, a clear local command centre, and twelve subtle abstract
  package-source tiles converging into one transparent status panel. Put exactly these
  crisp, high-legibility sans-serif text blocks on the quiet left third:
  “PUM” (largest), “Package Update Manager” (directly below), and “See what’s stale.
  Choose what changes.” (smallest). The text must be real readable typography, not
  pseudo-text. Visual message: clarity, consent, safe updates, no silent machine
  changes. Palette: deep graphite and midnight navy, soft amber for attention, mint for
  verified/current, restrained cobalt. High-end editorial product illustration,
  clean vector-meets-3D glass, crisp hierarchy. No logos, no brand marks, no code, no
  watermark, no close-up faces, no cyberpunk clichés, no lock icons, no clutter, and no
  extra text. Designed to stay readable as a GitHub social-card thumbnail.
  ```

  Regeneration gate: preserve the 2:1 crop; the three required text blocks must be
  legible at thumbnail size; no extra generated words or marks; keep the human/consent
  story visible. Upload this asset as the repository's GitHub social preview after the
  release is public.

- `social-preview.png` — previous 1280×640 social card, retained for comparison.
- `demo.gif` — terminal demo. Regenerate it only from real `pum` output after a release.
